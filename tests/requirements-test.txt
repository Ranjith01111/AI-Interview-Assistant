# ============================================
# Test Dependencies
# Install with: pip install -r tests/requirements-test.txt
# ============================================

pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
aiosqlite==0.20.0

# Already in main requirements.txt but listed for clarity:
# fastapi, sqlalchemy, pydantic, bcrypt, PyJWT, redis
